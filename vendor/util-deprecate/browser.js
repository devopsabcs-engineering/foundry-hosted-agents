
/**
 * Browser stub for `util.deprecate()`; matches the public `util-deprecate`
 * package's browser field. Not used by build-deck.js (Node-only script),
 * included only so bundlers resolving the "browser" field don't fail.
 */

module.exports = function deprecate(fn) {
  return fn;
};
